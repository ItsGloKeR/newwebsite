// This is the source file for the service worker.
// In a project with a build step (like Vite or Webpack), this file would be
// compiled into `service-worker.js`. Since this environment doesn't have a
// build step, the logic has been placed directly into `service-worker.js`.
